services:
  - type: web
    name: tardis-den
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NASA_API_KEY
        sync: false
      - key: ADMIN_PASSKEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
